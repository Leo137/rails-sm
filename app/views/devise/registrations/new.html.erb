<h2>Sign up</h2>

<div class="container">
  <div class="row">
    <div class="col-md-7">
<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, autofocus: true , class: 'form-control'%>
  </div>

  <div class="form-group">
    <%= f.label :server_id, "Server id" %><br>
    <%= f.number_field :server_id , class: 'form-control'%>
  </div>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email , class: 'form-control'%>
  </div>

  <div class="field">
    <%= f.label :password %>
    <% if @validatable %>
    <em>(<%= @minimum_password_length %> characters minimum)</em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "off" , class: 'form-control' %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, autocomplete: "off" , class: 'form-control' %>
  </div>
  </br>
  <div class="actions">
    <%= f.button :submit, "Sign up", class: 'btn-primary' %>
  </div>
<% end %>
</div>
<div class="col-md-5">
  <ul>
    <li>Please use your <b>stepmaniaonline.net</b> user id to sign up</li>
    <li>If you are unsure what it is, look at your profile page and write the last numbers near <b>"&uid="</b> at the URL.</li>
    <li>Example: http://stepmaniaonline.net/index.php?page=profile&uid=10156 => 10156.</li>
    <li>The name must be the same that your <b>stepmaniaonline.net</b> username.</li>
  </ul>
  
</div>
</div>
</div>

<%= render "devise/shared/links" %>
